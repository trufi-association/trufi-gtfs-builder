node_modules/
*.log
.DS_Store
*.tsbuildinfo

# Build output — produced by `npm run build` (auto via prepare in CI/CD).
dist/

# Example builds (regenerated by `npm run build` inside each example).
examples/*/dist/
trufi-gtfs-builder-*.tgz
